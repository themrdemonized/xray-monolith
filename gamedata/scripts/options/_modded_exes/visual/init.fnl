(var {: group} (import :.../builder))

(group :id :visual 
       (import :ui_hud)
       (import :crosshair)
       (import :3d_scopes)
       (import :particles)
       (import :wallmarks)
       (import :hdr10))
